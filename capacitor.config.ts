import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ph.flowerstore.fsinternal',
  appName: 'FS Internal System',
  webDir: 'dist'
};

export default config;
