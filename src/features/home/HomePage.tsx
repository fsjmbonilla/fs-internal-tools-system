export function HomePage() {
  return (
    <main className="flex min-h-dvh flex-col items-center justify-center gap-2">
      <h1 className="text-2xl font-semibold tracking-tight">FS Internal System</h1>
      <p className="text-sm text-muted-foreground">
        Foundation ready — auth and messaging arrive in the next phases.
      </p>
    </main>
  );
}
